name: adollarclass_flutter_app
description: "A new Flutter project."
publish_to: 'none' # Private package

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  firebase_core: ^4.2.1
  firebase_auth: ^6.1.2
  url_launcher: ^6.1.10 # <-- added for opening links
  google_sign_in: ^6.1.0
  cloud_firestore: ^6.1.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # Include all images in assets/images/
  assets:
    - assets/images/
